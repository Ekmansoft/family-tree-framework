// Legacy stub kept temporarily to prevent import errors in downstream code.
// This module is deprecated; connectors are now rendered inline in VerticalTreeView.
export default function DeprecatedVerticalConnectionLines() { return null; }
